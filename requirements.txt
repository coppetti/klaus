# IDE Agent Wizard - Dependencies
# ================================

# Core
pyyaml>=6.0
httpx>=0.25.0

# Telegram
python-telegram-bot>=20.6
aiohttp>=3.9.0

# Web Search (for real-time information)
duckduckgo-search>=3.9.0  # Web search without API key

# LLM Provider SDKs (required for multi-provider support)
anthropic>=0.8.0  # Anthropic SDK (used by Kimi provider)
google-genai>=1.0.0  # Google Gemini SDK (new, GA since May 2025)

# Hybrid Memory (optional but recommended)
kuzu>=0.4.0                    # Graph database for semantic relationships
sentence-transformers>=2.2.0   # Semantic embeddings (all-MiniLM-L6-v2, ~80MB, fully offline)

# Development
pytest>=7.4.0
pytest-asyncio>=0.21.0
