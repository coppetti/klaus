# Kimi Agent com suporte a AGENTS.md
# Estende clawd-agent e adiciona carregamento do AGENTS.md

FROM clawd-agent:latest

# Copiar app.py modificado
COPY app.py /app/kimi-agent/app.py

# O resto continua igual - uvicorn será iniciado pelo ENTRYPOINT/CMD original
